# Copyright 2026 Cloud Data Labs, Inc.
# SPDX-License-Identifier: Apache-2.0

name: Release from staging

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Release tag to create, for example v0.1.0"
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release-from-staging
  cancel-in-progress: false

jobs:
  release:
    name: Promote staging and create release
    runs-on: ubuntu-latest

    steps:
      - uses: actions/create-github-app-token@f8d387b68d61c58ab83c6c016672934102569859 # v3
        id: plugin-publisher-token
        with:
          app-id: ${{ vars.RESOLVE_PLUGIN_PUBLISH_APP_ID }}
          private-key: ${{ secrets.RESOLVE_PLUGIN_PUBLISH__PRIVATE_KEY }}
          owner: resolve-ai-oss
          repositories: resolve-plugin-base

      - name: Check out staging
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          ref: staging
          fetch-depth: 0
          token: ${{ steps.plugin-publisher-token.outputs.token }}

      - name: Configure Git
        run: |
          git config user.name "resolve-plugin-publisher[bot]"
          git config user.email "resolve-plugin-publisher[bot]@users.noreply.github.com"

      - name: Validate release tag
        env:
          RELEASE_TAG: ${{ inputs.release_tag }}
        shell: bash
        run: |
          if [[ ! "${RELEASE_TAG}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?$ ]]; then
            echo "release_tag must look like v1.2.3, with optional semver suffix."
            exit 1
          fi

          if git ls-remote --exit-code --tags origin "refs/tags/${RELEASE_TAG}" >/dev/null 2>&1; then
            echo "Tag ${RELEASE_TAG} already exists."
            exit 1
          fi

      - name: Promote staging to main
        run: git push origin HEAD:main

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ steps.plugin-publisher-token.outputs.token }}
          RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          release_target_sha="$(git rev-parse HEAD)"

          gh release create "${RELEASE_TAG}" \
            --title "${RELEASE_TAG}" \
            --target "${release_target_sha}" \
            --generate-notes
